# 📚 Подробный гайд по деплою на GitHub Pages (из ветки)

## Шаг 1: Подготовка репозитория на GitHub

1. **Создайте репозиторий на GitHub:**
   - Перейдите на [github.com](https://github.com)
   - Нажмите "New repository" (зеленая кнопка)
   - Назовите репозиторий (например: `liebevolle-haende-site`)
   - Выберите Public или Private
   - **НЕ** добавляйте README, .gitignore или лицензию (если проект уже существует локально)
   - Нажмите "Create repository"

2. **Инициализируйте Git в вашем проекте (если еще не сделано):**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. **Подключите удаленный репозиторий:**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
   git branch -M main
   git push -u origin main
   ```

## Шаг 2: Установка зависимостей

Установите пакет `gh-pages` для автоматического деплоя:

```bash
npm install
```

(Пакет `gh-pages` уже добавлен в `package.json`)

## Шаг 3: Настройка base path в vite.config.ts

**Важно:** Если ваш репозиторий называется НЕ `username.github.io`, нужно настроить base path.

1. Откройте файл `vite.config.ts`
2. Найдите строку с комментарием про base
3. Раскомментируйте и укажите правильное название репозитория:

```typescript
base: process.env.NODE_ENV === 'production' ? '/название-вашего-репозитория/' : '/',
```

**Пример:** Если репозиторий называется `liebevolle-haende-site`:
```typescript
base: process.env.NODE_ENV === 'production' ? '/liebevolle-haende-site/' : '/',
```

**Если репозиторий называется `username.github.io`** (специальное имя), оставьте base пустым или закомментированным.

## Шаг 4: Первый деплой

### Автоматический деплой (рекомендуется)

1. **Запустите команду деплоя:**
   ```bash
   npm run deploy
   ```

   Эта команда автоматически:
   - Соберет проект (`npm run build`)
   - Создаст ветку `gh-pages` (если её нет)
   - Загрузит файлы из папки `dist` в ветку `gh-pages`
   - Запушет изменения на GitHub

2. **Настройте GitHub Pages:**
   - Перейдите в ваш репозиторий на GitHub
   - Откройте **Settings** (Настройки)
   - В левом меню найдите **Pages**
   - В разделе **Source** выберите:
     - **Source:** `Deploy from a branch`
     - **Branch:** `gh-pages` → `/ (root)`
   - Нажмите **Save**

3. **Подождите 1-2 минуты** и проверьте ваш сайт:
   - URL будет: `https://ВАШ_USERNAME.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/`
   - Или посмотрите в **Settings → Pages** - там будет указан URL

## Шаг 5: Обновление сайта

После каждого изменения в коде:

1. **Закоммитьте изменения в main:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push
   ```

2. **Задеплойте обновления:**
   ```bash
   npm run deploy
   ```

   Подождите 1-2 минуты, и сайт обновится автоматически!

## 🔧 Ручной деплой (альтернатива)

Если автоматический деплой не работает, можно сделать вручную:

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Переключитесь на ветку gh-pages:**
   ```bash
   git checkout gh-pages
   ```
   
   Если ветки нет, создайте её:
   ```bash
   git checkout -b gh-pages
   ```

3. **Скопируйте файлы из dist в корень:**
   ```bash
   # Windows PowerShell
   Copy-Item -Path dist\* -Destination . -Recurse -Force
   
   # Linux/Mac
   cp -r dist/* .
   ```

4. **Закоммитьте и запушьте:**
   ```bash
   git add .
   git commit -m "Deploy to GitHub Pages"
   git push origin gh-pages
   ```

5. **Вернитесь на ветку main:**
   ```bash
   git checkout main
   ```

## 🔧 Решение проблем

### Проблема: Сайт показывает пустую страницу или 404

**Решение:**
1. Проверьте, правильно ли указан `base` в `vite.config.ts`
2. Убедитесь, что название репозитория в base совпадает с реальным названием
3. Подождите 1-2 минуты после деплоя (GitHub Pages может обновляться с задержкой)
4. Проверьте, что в Settings → Pages выбрана ветка `gh-pages` и папка `/ (root)`

### Проблема: Стили не загружаются

**Решение:**
1. Проверьте base path в `vite.config.ts`
2. Убедитесь, что все пути в коде относительные (начинаются с `/` или `./`)
3. Очистите кэш браузера (Ctrl+Shift+R или Cmd+Shift+R)

### Проблема: Команда `npm run deploy` не работает

**Решение:**
1. Убедитесь, что установлены все зависимости: `npm install`
2. Проверьте, что пакет `gh-pages` установлен: `npm list gh-pages`
3. Если ошибка с правами доступа, проверьте настройки Git:
   ```bash
   git config --global user.name "Ваше имя"
   git config --global user.email "ваш@email.com"
   ```

### Проблема: Ветка gh-pages не создается

**Решение:**
1. Убедитесь, что вы находитесь на ветке `main`:
   ```bash
   git checkout main
   ```
2. Попробуйте создать ветку вручную:
   ```bash
   git checkout -b gh-pages
   git push -u origin gh-pages
   git checkout main
   ```
3. Затем снова запустите `npm run deploy`

## 📝 Полезные ссылки

- [Документация GitHub Pages](https://docs.github.com/en/pages)
- [Документация Vite для деплоя](https://vitejs.dev/guide/static-deploy.html#github-pages)
- [Документация gh-pages](https://github.com/tschaub/gh-pages)

## ✅ Чеклист перед деплоем

- [ ] Репозиторий создан на GitHub
- [ ] Код загружен в репозиторий (ветка `main`)
- [ ] Зависимости установлены (`npm install`)
- [ ] `base` настроен в `vite.config.ts` (если нужно)
- [ ] Команда `npm run deploy` выполнена успешно
- [ ] В Settings → Pages выбрано: `Deploy from a branch` → `gh-pages` → `/ (root)`
- [ ] Сайт доступен по указанному URL

---

**После успешного деплоя ваш сайт будет доступен по адресу:**
`https://ВАШ_USERNAME.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/`

**Для обновления сайта просто запускайте:** `npm run deploy`
