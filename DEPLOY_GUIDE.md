# 📚 Подробный гайд по деплою на GitHub Pages

## Шаг 1: Подготовка репозитория на GitHub

1. **Создайте репозиторий на GitHub:**
   - Перейдите на [github.com](https://github.com)
   - Нажмите "New repository" (зеленая кнопка)
   - Назовите репозиторий (например: `liebevolle-haende-site`)
   - Выберите Public или Private
   - **НЕ** добавляйте README, .gitignore или лицензию (если проект уже существует локально)
   - Нажмите "Create repository"

2. **Инициализируйте Git в вашем проекте (если еще не сделано):**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. **Подключите удаленный репозиторий:**
   ```bash
   git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
   git branch -M main
   git push -u origin main
   ```

## Шаг 2: Настройка base path в vite.config.ts

**Важно:** Если ваш репозиторий называется НЕ `username.github.io`, нужно настроить base path.

1. Откройте файл `vite.config.ts`
2. Найдите строку с комментарием про base
3. Раскомментируйте и укажите правильное название репозитория:

```typescript
base: process.env.NODE_ENV === 'production' ? '/название-вашего-репозитория/' : '/',
```

**Пример:** Если репозиторий называется `liebevolle-haende-site`:
```typescript
base: process.env.NODE_ENV === 'production' ? '/liebevolle-haende-site/' : '/',
```

**Если репозиторий называется `username.github.io`** (специальное имя), оставьте base пустым или закомментированным.

## Шаг 3: Загрузка кода на GitHub

**Важно:** Сначала загрузите код с workflow файлом, иначе опция "GitHub Actions" не появится!

```bash
git add .
git commit -m "Setup GitHub Pages deployment"
git push
```

## Шаг 4: Включение GitHub Pages в настройках репозитория

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** (Настройки)
3. В левом меню найдите **Pages**
4. **Подождите 1-2 минуты** после push (GitHub нужно время, чтобы обнаружить workflow)
5. В разделе **Source** выберите:
   - **Source:** `GitHub Actions` (эта опция появится после загрузки workflow файла)
6. Если опции "GitHub Actions" еще нет:
   - Убедитесь, что файл `.github/workflows/deploy.yml` загружен в репозиторий
   - Проверьте вкладку **Actions** - там должен быть запущен workflow
   - После первого успешного запуска опция появится автоматически

## Шаг 5: Деплой

### Автоматический деплой (рекомендуется)

1. **Загрузите код на GitHub:**
   ```bash
   git add .
   git commit -m "Setup GitHub Pages deployment"
   git push
   ```

2. **Проверьте статус деплоя:**
   - Перейдите в репозиторий на GitHub
   - Откройте вкладку **Actions**
   - Вы увидите процесс деплоя
   - Дождитесь завершения (зеленая галочка)

3. **Найдите ваш сайт:**
   - После успешного деплоя перейдите в **Settings → Pages**
   - Там будет указан URL вашего сайта
   - Обычно это: `https://ВАШ_USERNAME.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/`

### Ручной деплой (альтернатива)

Если автоматический деплой не работает, можно использовать ручной:

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Создайте ветку gh-pages:**
   ```bash
   git checkout -b gh-pages
   ```

3. **Переместите файлы из dist в корень:**
   ```bash
   # Windows PowerShell
   Copy-Item -Path dist\* -Destination . -Recurse -Force
   
   # Или вручную скопируйте все файлы из папки dist в корень проекта
   ```

4. **Закоммитьте и запушьте:**
   ```bash
   git add .
   git commit -m "Deploy to GitHub Pages"
   git push origin gh-pages
   ```

5. **В настройках репозитория (Settings → Pages) выберите:**
   - **Source:** `Deploy from a branch`
   - **Branch:** `gh-pages` → `/ (root)`

## Шаг 6: Обновление сайта

После каждого изменения в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

GitHub Actions автоматически соберет и задеплоит новую версию сайта.

## 🔧 Решение проблем

### Проблема: Сайт показывает пустую страницу или 404

**Решение:**
1. Проверьте, правильно ли указан `base` в `vite.config.ts`
2. Убедитесь, что название репозитория в base совпадает с реальным названием
3. Подождите 1-2 минуты после деплоя (GitHub Pages может обновляться с задержкой)

### Проблема: Стили не загружаются

**Решение:**
1. Проверьте base path в `vite.config.ts`
2. Убедитесь, что все пути в коде относительные (начинаются с `/` или `./`)

### Проблема: GitHub Actions не запускается

**Решение:**
1. Проверьте, что файл `.github/workflows/deploy.yml` существует
2. Убедитесь, что в Settings → Pages выбран источник "GitHub Actions"
3. Проверьте, что вы пушите в ветку `main` (или `master`)

## 📝 Полезные ссылки

- [Документация GitHub Pages](https://docs.github.com/en/pages)
- [Документация Vite для деплоя](https://vitejs.dev/guide/static-deploy.html#github-pages)
- [GitHub Actions документация](https://docs.github.com/en/actions)

## ✅ Чеклист перед деплоем

- [ ] Репозиторий создан на GitHub
- [ ] Код загружен в репозиторий
- [ ] `base` настроен в `vite.config.ts` (если нужно)
- [ ] GitHub Pages включен в настройках репозитория
- [ ] Источник выбран как "GitHub Actions"
- [ ] Файл `.github/workflows/deploy.yml` создан
- [ ] Изменения запушены в репозиторий
- [ ] Деплой успешно завершен (проверено в Actions)

---

**После успешного деплоя ваш сайт будет доступен по адресу:**
`https://ВАШ_USERNAME.github.io/НАЗВАНИЕ_РЕПОЗИТОРИЯ/`

